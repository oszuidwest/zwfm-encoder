---
# Hybrid: `go` delegates to go-ci.yml; `frontend` stays inline for the
# dependabot lockfile-commit step.
name: CI

on:
  push:
    branches: ['main']
    paths:
      - '**.go'
      - 'go.mod'
      - 'go.sum'
      - '.golangci.yml'
      - '.golangci.yaml'
      - 'web/**'
      - 'package.json'
      - 'biome.json'
      - '.htmlhintrc'
  pull_request:
    branches: ['main']
    paths:
      - '**.go'
      - 'go.mod'
      - 'go.sum'
      - '.golangci.yml'
      - '.golangci.yaml'
      - 'web/**'
      - 'package.json'
      - 'biome.json'
      - '.htmlhintrc'

permissions:
  contents: read

jobs:
  go:
    uses: oszuidwest/.github-templates/.github/workflows/go-ci.yml@v2
    with:
      golangci-lint-version: v2.12.1

  frontend:
    name: Frontend
    runs-on: ubuntu-latest
    permissions:
      contents: write  # commit refreshed bun.lockb on dependabot PRs

    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.head_ref }}
          token: ${{ github.token }}

      - uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install ${{ github.actor != 'dependabot[bot]' && '--frozen-lockfile' || '' }}

      - name: Commit updated lockfile
        if: github.actor == 'dependabot[bot]'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add bun.lockb
          git diff --staged --quiet || git commit -m "chore: update bun.lockb"
          git push

      - name: Lint
        run: bun run lint
